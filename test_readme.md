# 模型推理测试脚本说明

本文档说明了如何使用提供的测试脚本来验证新训练的模型是否能正常运行。

## 1. 测试脚本功能

### test_inference.py
- **功能**: 简单的模型推理测试脚本，直接调用模型进行推理
- **适用场景**: 快速验证模型是否能正常加载和运行
- **特点**: 不使用信号槽机制，结构简单，便于调试

### test_inference_improved.py
- **功能**: 完整的模型推理测试脚本，使用PyQt信号槽机制模拟实际运行环境
- **适用场景**: 更接近实际应用环境的测试，验证整个推理流程
- **特点**: 包含完整的视频处理、模型推理、后处理流程

## 2. 使用方法

### 环境要求
- Python 3.8+
- PyQt6
- OpenCV
- CUDA (可选，用于GPU加速)
- 其他项目依赖包

### 安装依赖
```bash
pip install opencv-python numpy pyqt6
```

### 运行测试脚本

#### 方法1: 运行简单测试脚本
```bash
python test_inference.py
```

#### 方法2: 运行完整测试脚本
```bash
python test_inference_improved.py
```

### 输入参数
运行脚本后，会提示输入以下信息：
1. **测试视频文件路径**: 输入要测试的视频文件的完整路径
2. **新训练的模型路径**: 输入新训练的模型文件（.pt格式）的完整路径

## 3. 测试内容

测试脚本会验证以下内容：

1. **模型加载**: 验证模型是否能成功加载
2. **类别处理**: 验证模型是否能正确处理bare和wear两个类别
3. **推理功能**: 验证模型是否能正常进行推理
4. **结果解析**: 验证后处理是否能正确解析模型输出
5. **性能测试**: 显示每帧的处理时间和平均FPS

## 4. 输出信息

测试过程中会输出以下信息：
- 视频文件信息（总帧数、FPS）
- 每帧的处理进度
- 检测结果（类别、置信度、边界框）
- 后处理结果（是否需要告警、危险检测情况）
- 性能统计（平均处理时间、平均FPS）

## 5. 注意事项

1. **确保模型路径正确**: 新训练的模型必须是YOLO格式的.pt文件
2. **确保视频文件可访问**: 测试视频文件必须能被正确打开
3. **CUDA支持**: 如果需要使用GPU加速，请确保CUDA已正确安装
4. **类别名称**: 新训练的模型类别必须包含bare和wear，与脚本中的配置一致
5. **测试时长**: 较长的视频会导致测试时间较长，可以选择较短的视频片段

## 6. 故障排除

### 问题1: 模型加载失败
- 检查模型路径是否正确
- 检查模型格式是否为YOLO格式的.pt文件
- 检查模型是否支持当前的YOLO版本

### 问题2: 检测结果为空
- 检查视频中是否包含需要检测的目标
- 检查置信度阈值是否设置过高
- 检查模型是否正确训练

### 问题3: 性能较差
- 尝试降低输入分辨率
- 启用CUDA加速
- 检查CPU/GPU资源使用情况

## 7. 示例输出

```
=== 模型推理测试程序 ===
此程序用于测试新训练的模型是否能正常运行
支持bare和wear两个类别

请输入测试视频文件路径: test_video.mp4
请输入新训练的模型路径: new_model.pt

初始化检测模型...
初始化推理线程...
初始化后处理器...

打开视频文件: test_video.mp4
视频信息: 总帧数=100, FPS=30

处理第 1/100 帧
推理完成，处理时间: 0.0456秒
检测结果:
  glove模型检测到 2 个目标:
    - 类别: bare, 置信度: 0.9543, 框: (123, 45, 234, 156)
    - 类别: wear, 置信度: 0.8921, 框: (345, 67, 456, 178)
后处理结果: need_alert=False
危险检测: False

处理第 2/100 帧
...
```

## 8. 联系信息

如果测试过程中遇到问题，请检查模型训练过程或联系技术支持。